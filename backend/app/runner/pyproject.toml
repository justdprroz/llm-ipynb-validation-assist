[project]
name = "gradelab-runner"
version = "0.1.0"
description = "GradeLab pipeline runner shim"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
