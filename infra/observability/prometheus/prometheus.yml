global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
        labels:
          service: otel-collector

  # Add app targets when services expose /metrics, e.g.:
  # - job_name: gradelab-backend
  #   static_configs:
  #     - targets: ["backend:8000"]
